"""AURA Business Workflows — automated reports and triage.

Workflows run on schedule (via APScheduler) or on-demand via Telegram commands.
Each workflow gathers data (zero-token) and formats a Telegram-ready report.
"""
